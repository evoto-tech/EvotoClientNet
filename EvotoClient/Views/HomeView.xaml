<UserControl x:Class="EvotoClient.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EvotoClient.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"

             DataContext="{Binding Home, Source={StaticResource Locator}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock>You are home</TextBlock>

        <ListBox Grid.Row="1" SelectedItem="{Binding SelectedVote}" ItemsSource="{Binding Votes}" />
        <Button Grid.Row="2" Command="{Binding ProceedCommand}">Proceed</Button>
        <Button Grid.Row="3" Command="{Binding RefreshCommand}">Refresh</Button>
    </Grid>
</UserControl>